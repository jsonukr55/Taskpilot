{
  "name": "taskpilot-functions",
  "description": "TaskPilot Firebase Cloud Functions",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0",
    "groq-sdk": "^1.1.2"
  },
  "devDependencies": {
    "typescript": "^5.4.2"
  },
  "private": true
}
